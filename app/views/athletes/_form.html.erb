<%= form_with(model: athlete, local: true) do |form| %>
  <% if athlete.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(athlete.errors.count, "error") %> prohibited this athlete from being saved:</h2>

      <ul>
      <% athlete.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :athlete_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :athlete_last_name %>
  </div>

  <div class="field">
    <%= form.label :birthdate %>
    <%= form.datetime_select :birthdate, id: :athlete_birthdate %>
  </div>

  <div class="field">
    <%= form.label :male %>
    <%= form.check_box :male, id: :athlete_male %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city, id: :athlete_city %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
